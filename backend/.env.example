# Copy this file to .env and fill in values if you enable a real LLM
# in get_llm_reply() inside main.py. Not required to run the backend
# as-is (rule-based chat + JSON contact storage need no keys).

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
